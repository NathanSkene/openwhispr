---
phase: 01-upstream-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [SYNC-01, SYNC-02]

must_haves:
  truths:
    - "Local main contains all 17 upstream commits (merged, conflicts resolved)"
    - "origin/main matches local main (push succeeded)"
    - "App builds and installs successfully after merge"
    - "Uncommitted WIP files are preserved exactly as they were before merge"
  artifacts:
    - path: "git log"
      provides: "17 upstream commits merged into local main"
    - path: "/Applications/OpenWhispr Dev.app"
      provides: "Working installed app after merge"
  key_links:
    - from: "upstream/main"
      to: "local main"
      via: "git merge"
      pattern: "git log upstream/main..HEAD"
    - from: "local main"
      to: "origin/main"
      via: "git push"
      pattern: "git log origin/main..HEAD"
---

<objective>
Merge 17 upstream commits into local main, push to origin, and verify the app still builds.

Purpose: The fork must be current with upstream before any feature branches are created (Phase 2 depends on this clean base).

Output: Local main and origin/main both contain all upstream commits. App builds and runs. WIP files preserved.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

CRITICAL CONSTRAINTS (from project CLAUDE.md and STATE.md):
- NEVER run `npm start` — build with `npm run build:dev`
- The installed app is `/Applications/OpenWhispr Dev.app`
- Must kill running app before replacing it
- 16 modified + 2 new uncommitted WIP files on main MUST be preserved (they belong to Phase 2)
- Use `/openwhispr build` workflow: kill app -> npm run build:dev -> codesign -> cp to /Applications

WIP FILES TO PRESERVE (uncommitted on main):
- .gitignore, CLAUDE.md, package.json
- src/components/RecordingOverlay.css, src/components/SettingsPage.tsx
- src/components/notes/DictationWidget.tsx, src/config/promptData.json
- src/helpers/audioManager.js, src/helpers/mediaPlayer.js
- src/helpers/windowConfig.js, src/helpers/windowManager.js
- src/hooks/useSettings.ts, src/index.css
- src/locales/en/prompts.json, src/locales/en/translation.json
- src/stores/settingsStore.ts
- NEW FILES: AGENTS.md, electron-builder.dev.json, src/helpers/vadProcessor.js

FILES WITH POTENTIAL MERGE CONFLICTS (modified in both WIP and upstream):
- src/components/RecordingOverlay.css
- src/components/SettingsPage.tsx
- src/helpers/audioManager.js
- src/helpers/windowConfig.js
- src/helpers/windowManager.js
- src/locales/en/translation.json
- src/stores/settingsStore.ts

Note: These 7 files are modified in uncommitted WIP AND differ between HEAD and upstream/main.
The stash-merge-unstash flow will likely produce conflicts on unstash. The executor must
resolve these by keeping WIP changes on top of the merged upstream state.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stash WIP and merge upstream into main</name>
  <files></files>
  <read_first>
    - Run `git status` to confirm current uncommitted state
    - Run `git log --oneline HEAD..upstream/main` to confirm 17 commits to merge
    - Run `git stash list` to confirm no existing stashes
  </read_first>
  <action>
Step 1 — Fetch latest upstream:
```
git fetch upstream
```

Step 2 — Record WIP state for later verification:
```
git diff --stat > /tmp/openwhispr-wip-stat.txt
git diff > /tmp/openwhispr-wip-full.patch
git status --porcelain > /tmp/openwhispr-wip-files.txt
```

Step 3 — Stash ALL uncommitted changes (tracked modifications + untracked new files):
```
git stash push -u -m "Phase 2 WIP: VAD + max duration + overlay refinements"
```
Verify stash saved: `git stash list` should show exactly 1 entry.
Verify working tree is clean: `git status` should show nothing to commit.

Step 4 — Merge upstream/main into local main:
```
git merge upstream/main -m "merge: sync with upstream (17 commits)"
```
This should complete without conflicts because the working tree is clean and our 6 local commits
(4 features + 2 docs) should not conflict with upstream's 17 commits. The features we added
(recording overlay + BYOK chunking) touch different code than upstream's fixes.

If there ARE merge conflicts: resolve them by examining each conflict. Our commits added new
features (recording overlay, BYOK chunking, bulk dictionary import). Upstream fixed existing
features (KDE Wayland paste, GNOME hotkeys, Windows paste, streaming STT, etc.). In most cases
both sides should be kept. Use `git diff --name-only --diff-filter=U` to list conflicted files.

Step 5 — Verify merge succeeded:
```
git log --oneline HEAD..upstream/main
```
This must return empty output (0 commits ahead of local).

Step 6 — Restore WIP from stash:
```
git stash pop
```

If stash pop produces conflicts (expected for the 7 overlapping files listed in context):
- For each conflicted file, the WIP version (ours/stash) contains Nathan's Phase 2 work
- The merged version (theirs/HEAD) contains the upstream changes
- Resolution strategy: KEEP BOTH — upstream changes are bug fixes to existing code,
  WIP changes are new feature additions. They should coexist.
- After resolving, run `git checkout --theirs` is WRONG here. Manually merge each conflict.
- Run `git diff --stat` and compare with `/tmp/openwhispr-wip-stat.txt` to verify WIP is preserved.

Step 7 — Verify WIP files are all present:
```
git status --porcelain
```
Compare with `/tmp/openwhispr-wip-files.txt`. All 16 modified + 2 new files must still appear.

Step 8 — Push to origin:
```
git push origin main
```
  </action>
  <verify>
    <automated>cd /Users/nathanskene/Projects/openwhispr && git log --oneline upstream/main..HEAD | wc -l | tr -d ' ' && echo "---" && git log --oneline HEAD..upstream/main | wc -l | tr -d ' ' && echo "---" && git status --porcelain | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline HEAD..upstream/main` returns empty (0 lines) — all 17 upstream commits merged
    - `git log --oneline upstream/main..HEAD` returns 7 or more lines (6 original + 1 merge commit)
    - `git status --porcelain` shows 18 lines (16 modified + 2 untracked) — WIP preserved
    - `git push origin main` completed successfully (no error)
    - `git stash list` is empty (stash was popped)
    - All 7 potentially conflicted files still appear in `git status --porcelain`: RecordingOverlay.css, SettingsPage.tsx, audioManager.js, windowConfig.js, windowManager.js, translation.json, settingsStore.ts
  </acceptance_criteria>
  <done>Local main contains all upstream commits. origin/main matches local main. All WIP files preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Build, install, and verify app works</name>
  <read_first>
    - Read /Users/nathanskene/Projects/openwhispr/package.json to confirm build:dev script exists
  </read_first>
  <action>
Step 1 — Kill any running OpenWhispr Dev:
```
pkill -f "OpenWhispr Dev" 2>/dev/null || true
```

Step 2 — Install dependencies (upstream may have added/changed deps):
```
cd /Users/nathanskene/Projects/openwhispr && npm install
```

Step 3 — Build the app:
```
npm run build:dev
```

Step 4 — Codesign the built app:
```
codesign --force --deep --sign "OpenWhispr Dev" dist/mac-arm64/OpenWhispr\ Dev.app
```
(Adjust path if the build output is at a different location — check `ls dist/` first.)

Step 5 — Install to /Applications:
```
cp -R dist/mac-arm64/OpenWhispr\ Dev.app /Applications/
```

Step 6 — Launch the app:
```
open "/Applications/OpenWhispr Dev.app"
```
  </action>
  <what-built>Merged 17 upstream commits and rebuilt the app with all upstream fixes (KDE Wayland paste, GNOME hotkeys, Windows paste, streaming STT, Google OAuth, etc.)</what-built>
  <how-to-verify>
    1. The app launches without crashing
    2. Open the control panel (click the OpenWhispr Dev icon or use the menu)
    3. Verify settings load correctly
    4. Press the hotkey (Globe key) — app should respond to recording toggle
    5. If you have an OpenAI API key configured, do a quick test dictation to verify transcription still works
  </how-to-verify>
  <resume-signal>Type "approved" if the app works, or describe any issues</resume-signal>
  <verify>
    <automated>ls -la "/Applications/OpenWhispr Dev.app/Contents/MacOS/" 2>/dev/null | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `/Applications/OpenWhispr Dev.app` exists and has recent modification time (today's date)
    - `npm run build:dev` completed with exit code 0
    - App launches without crash
    - User confirms app is functional
  </acceptance_criteria>
  <done>App builds, installs, and runs successfully after upstream merge. User has verified functionality.</done>
</task>

</tasks>

<verification>
1. `git log --oneline HEAD..upstream/main` returns empty — all upstream merged
2. `git log --oneline upstream/main..HEAD` shows 7+ commits — our commits preserved
3. `git diff origin/main HEAD` returns empty — push succeeded
4. `git status --porcelain | wc -l` returns 18 — WIP preserved
5. `/Applications/OpenWhispr Dev.app` exists with today's date
</verification>

<success_criteria>
- All 17 upstream commits merged into local main
- origin/main matches local main (push succeeded)
- Uncommitted WIP (16 modified + 2 new files) preserved exactly
- App builds via `npm run build:dev` without errors
- Installed app launches and functions (user verified)
</success_criteria>

<output>
After completion, create `.planning/phases/01-upstream-sync/01-01-SUMMARY.md`
</output>
